apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demobackend.fullname" . }}-test-connection"
  labels:
    {{- include "demobackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demobackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
