apiVersion: v1
kind: Service
metadata:
  name: {{ include "demobackend.fullname" . }}
  labels:
    {{- include "demobackend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demobackend.selectorLabels" . | nindent 4 }}
